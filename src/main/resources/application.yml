spring:
  application:
    name: spring-ai-demo
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}
      model: gpt-4

logging:
  level:
    root: INFO
    com.example: DEBUG
    org.springframework.ai: DEBUG